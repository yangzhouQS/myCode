class Productor {
  constructor(pname, price) {
    this.pname = pname;
    this.price = price;
  }

}

//类的实例化
let p = new Productor('牙膏', 16.9);
// Productor();//Class constructor Productor cannot be invoked without 'new'

