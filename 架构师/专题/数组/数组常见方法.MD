```
构造函数
constructor:
获取长度属性
length:


转换为字符串
toLocaleString: ƒ toLocaleString()
toString: 返回一个字符串，表示指定的数组及其元素。
toLocaleString:返回一个字符串表示数组中的元素。


数组常用方法
pop: 
push: 
shift:
unshift:
reverse: 
slice:
sort:
splice: 
includes: 
indexOf: 
lastIndexOf:
join:
concat:
find:
findIndex:
copyWithin: 
fill:
valueOf:


数组高阶函数
forEach:
map:
filter:
reduce:
reduceRight:
some:
every:

展平数组
flat: ƒ flat()
flatMap: ƒ flatMap()

数组遍历的方法
entries: ƒ entries()
keys: ƒ keys()
values: ƒ values()

```
